# Rust 构建产物
/target

# AI 编程助手的工作目录：里面是会话记忆和草稿，不属于项目源码。
.workbuddy/

# `.workbuddy-ai` 是指向 `.workbuddy` 的目录联接（两个客户端共用同一份内容），
# 一并忽略 —— 免得同一个目录在仓库里出现两遍。
# ⚠️ 两种写法都要留 —— 实测行为**随 git 所在平台而变**：
#   · WSL / Linux 的 git（经 `/mnt/d` 访问）把联接看成「条目」而不是目录
#     （`git status` 显示 `?? .workbuddy-ai`，无尾斜杠）→ 只有**不带斜杠**的
#     `.workbuddy-ai` 能匹配，`.workbuddy-ai/` 是静默失效的；
#   · Windows 原生 git 把联接当目录 → 两种写法都能匹配。
.workbuddy-ai
.workbuddy-ai/
