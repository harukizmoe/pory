# Rust 构建产物
/target

# AI 编程助手的工作目录：里面是会话记忆和草稿，不属于项目源码。
.workbuddy/

# `.workbuddy-ai` 是指向 `.workbuddy` 的目录联接（两个客户端共用同一份内容），
# 一并忽略 —— 免得同一个目录在仓库里出现两遍。
# ⚠️ 两种写法都要留：带斜杠的模式只匹配「目录」，而联接在 git 眼里是一个条目
# 而不是目录（`git status` 里显示为 `?? .workbuddy-ai` 而非 `.workbuddy-ai/`），
# 所以 `.workbuddy-ai/` 匹配不上它 —— 实测过，只写带斜杠那条会漏。
.workbuddy-ai
.workbuddy-ai/
